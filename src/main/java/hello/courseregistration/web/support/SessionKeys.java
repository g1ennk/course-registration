package hello.courseregistration.web.support;

/** web(SSR) 계층에서 쓰는 세션 속성 키 모음. 문자열 중복/오타를 막는다. */
public final class SessionKeys {

    public static final String USER_ID = "userId";

    private SessionKeys() {
    }
}
